server {
    listen 8080;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Allow file uploads up to 12MB (Express multer limit is 10MB)
    client_max_body_size 12m;

    # Gzip compression
    gzip on;
    gzip_types text/plain text/css application/json application/javascript text/xml application/xml application/xml+rss text/javascript image/svg+xml;
    gzip_min_length 256;

    # Use Docker's embedded DNS so nginx defers "server" hostname resolution to
    # request time. Without this, nginx fails to start if "server" is not yet
    # resolvable (e.g. standalone smoke tests without compose network).
    resolver 127.0.0.11 valid=10s ipv6=off;
    set $upstream_server http://server:5000;

    # Proxy API requests to Express server
    location /api/ {
        proxy_pass $upstream_server;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 120s;
    }

    # Proxy uploaded files to Express server
    location /uploads/ {
        proxy_pass $upstream_server;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # Proxy health check to Express server
    location = /health {
        proxy_pass $upstream_server;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
    }

    # SPA fallback — serve index.html for all non-file routes
    location / {
        try_files $uri $uri/ /index.html;
    }

    # Cache static assets (Vite content-hashes filenames, so immutable is safe)
    location ~* \.(js|css|png|jpg|jpeg|gif|ico|svg|woff|woff2|ttf|eot)$ {
        add_header Cache-Control "public, max-age=604800, immutable";
    }
}
