# npm audit baseline — 2026-04-17

Baseline audit run against production dependencies only (`npm audit --production`).
This is a documentation snapshot, not a fix-everything pass.

---

## Server (`server/`)

```
7 vulnerabilities (2 moderate, 5 high)
```

| Package | Severity | Advisory | Notes |
|---------|----------|----------|-------|
| `lodash <=4.17.23` | High | GHSA-r5fr-rjxr-66jc, GHSA-f23m-r3pf-42rh | Code injection via `_.template`; prototype pollution. Transitive dep (via `readdir-glob`). Not directly used. |
| `multer <=2.1.0` | High | GHSA-xf7r-hgr6-v32p, GHSA-v52c-386h-88mc, GHSA-5528-5vmv-3xc2 | DoS via incomplete cleanup / uncontrolled recursion. Directly used for file uploads. `npm audit fix` should resolve. |
| `nodemailer <=8.0.4` | Moderate | GHSA-vvjj-xcjg-gr5g | SMTP command injection via CRLF in transport name. Directly used for magic-link email. `npm audit fix` should resolve. |
| `path-to-regexp 8.0.0 - 8.3.0` | High | GHSA-j3q9-mxjg-w52f, GHSA-27v5-c462-wpq7 | ReDoS via sequential optional groups / multiple wildcards. Transitive dep (via Express 5). |
| `underscore <=1.13.7` | High | GHSA-qpx9-hpmf-5gmw | Unlimited recursion in `_.flatten`/`_.isEqual`, potential DoS. Transitive dep. |

**Action items:**
- Run `npm audit fix` in `server/` — all issues have auto-fixes available.
- After fix, verify `multer` and `nodemailer` still work (upload + magic-link flows).
- `path-to-regexp` fix depends on Express 5 releasing a patch — monitor upstream.

---

## Client (`client/`)

```
2 vulnerabilities (2 moderate)
```

| Package | Severity | Advisory | Notes |
|---------|----------|----------|-------|
| `axios 1.0.0 - 1.14.0` | Moderate | GHSA-3p68-rc4w-qgx5, GHSA-fvcv-3m26-pcqx | NO_PROXY bypass (SSRF); header injection chain. Directly used for API calls. `npm audit fix` should resolve. |
| `follow-redirects <=1.15.11` | Moderate | GHSA-r4q5-vmmm-2653 | Custom auth headers leaked to cross-domain redirects. Transitive dep (via `axios`). |

**Action items:**
- Run `npm audit fix` in `client/` — all issues have auto-fixes available.
- After fix, verify API calls still work end-to-end.

---

## Summary

| Scope | High | Moderate | Low | Total |
|-------|------|----------|-----|-------|
| server | 5 | 2 | 0 | 7 |
| client | 0 | 2 | 0 | 2 |
| **Total** | **5** | **4** | **0** | **9** |

All 9 vulnerabilities have `npm audit fix` auto-fixes available. No manual version pinning required at this time.
Next audit review should be scheduled within 30 days or after any dependency update.
